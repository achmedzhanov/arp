

Важно

- QuickFix заменить на Actipon (диалог при запуске Action? ака reformat code)
- Проверить с вролеженными типами (что будет с регионами вложденных типов)
- Обощить для раьботы с интерфейсами, структырами
- Поддержка Event Handlers
- учитывать существующие регионы (например:
-- если есть region AnyProperties с группированными свойствами, то AnyProperties оказывается внутри региона Properties
-- в диалоге с опциями перед упорядочиванием пользователь может выбрать какие регионы не трогать)
- удалять опустевшие регионы

Желательно

- упорядочивание но без region (! это важно, думаю что будет более часто использоваться чем упорядочивание с регионами)
- UI для включения, отключения группировок
- сортировка полей (в алфовитно графич., умная: учет префикосв ака Get Set Enable Disable etc )
- некорректный код неопределенно перемещается в релионе после группировки
- шруппировка для Dependency Property
- сохранение опций упорядочивания для фалов после запуска упорядочивания


